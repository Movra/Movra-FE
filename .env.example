VITE_API_BASE_URL=http://localhost:8080

# Visual verification mode is dev-only.
# `npm run dev:visual` loads `.env.visual`, which points the app at
# `npm run mock:visual` and enables a non-persistent visual auth fallback.
# Do not copy `VITE_VISUAL_AUTH=1` into production-like env files.
